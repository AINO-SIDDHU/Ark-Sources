import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EnterpriseMetaConfigComponent } from './enterprise-meta-config.component';

describe('EnterpriseMetaConfigComponent', () => {
  let component: EnterpriseMetaConfigComponent;
  let fixture: ComponentFixture<EnterpriseMetaConfigComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EnterpriseMetaConfigComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EnterpriseMetaConfigComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
