import { BehaviorSubject } from "rxjs/internal/BehaviorSubject";
export declare class MetaConfig {
    metaConfigHeader: string;
    /** makes the @currentFormHeader as an observable, which emits the current form header(updated header)
    *   and can be subscribed by any component.
    */
    metaConfigHeaderSource: BehaviorSubject<string>;
    currentMetaConfigHeader: import("rxjs").Observable<string>;
    readonly getMetaConfigHeader: string;
    setMetaConfigHeader: string;
}
