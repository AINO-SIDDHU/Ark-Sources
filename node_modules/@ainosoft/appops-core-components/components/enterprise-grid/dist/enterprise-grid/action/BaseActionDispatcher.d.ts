import { ActionDispatcher } from "./ActionDispatcher";
export declare class BaseActionDispatcher implements ActionDispatcher {
    actionDispatcher: string;
    constructor();
    /**
     * Get the token convert it and call the method requested .
     */
    dispatchAction(token: string): void;
    readonly getActionDispatcher: string;
    setActionDispatcher: any;
}
