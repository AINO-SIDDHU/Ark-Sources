import { OnInit } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { EnterpriseFormService } from './enterprise-form.service';
import { FormConfig } from '../config/FormConfig';
import { ActionConfig } from '../config/ActionConfig';
export declare class EnterpriseFormComponent implements OnInit {
    private _formBuilder;
    private enterpFormService;
    private formConfig;
    private actionConfig;
    firstFormGroup: FormGroup;
    secondFormGroup: FormGroup;
    isEditable: boolean;
    isLinear: boolean;
    enableGrid: boolean;
    formHeader: string;
    referencePackageList: any[];
    enableModuleName: boolean;
    enableUserRole: boolean;
    enableRefPackage: boolean;
    enableBasePrice: boolean;
    enableSubFrequency: boolean;
    enableMetaConfig: boolean;
    constructor(_formBuilder: FormBuilder, enterpFormService: EnterpriseFormService, formConfig: FormConfig, actionConfig: ActionConfig);
    ngOnInit(): void;
    backToTable(): void;
    openMetaConfig(): void;
}
