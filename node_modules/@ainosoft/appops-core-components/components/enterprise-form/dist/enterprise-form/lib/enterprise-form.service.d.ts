import { BehaviorSubject } from 'rxjs';
import { FormConfig } from '../config/FormConfig';
import { ActionConfig } from '../config/ActionConfig';
export declare class EnterpriseFormService {
    private formConfigComp?;
    private actionConfigComp?;
    list: String[];
    enableGrid: boolean;
    enableMetaConfig: boolean;
    constructor(formConfigComp?: FormConfig, actionConfigComp?: ActionConfig);
    /** makes the @currentList as an observable, which emits the current list(updated list)
    *  and can be subscribed by any component.
   */
    moduleListSource: BehaviorSubject<any[]>;
    currentList: import("rxjs").Observable<any[]>;
    enableGridSource: BehaviorSubject<boolean>;
    currentEnableGridValue: import("rxjs").Observable<boolean>;
    enableMetaConfigSource: BehaviorSubject<boolean>;
    currentEnableMetaConfigValue: import("rxjs").Observable<boolean>;
    /** sets the grid list/data provided by the respective external modules(apps/plugins). */
    setFormList(data: any[]): void;
    enableOpenGridAction(value: boolean): void;
    /** sets the form header/name provided from their respective external modules(apps/plugins). */
    setFormHeader(formHeader: string): void;
    enableModuleName(value: boolean): void;
    enableUserRole(value: boolean): void;
    enableRefPackage(value: boolean): void;
    enableBasePrice(value: boolean): void;
    enableSubFrequency(value: boolean): void;
    enableOpenMetaConfigAction(value: boolean): void;
}
