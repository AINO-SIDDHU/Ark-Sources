import { BehaviorSubject } from "rxjs/internal/BehaviorSubject";
export declare class FormConfig {
    formHeader: string;
    /** makes the @currentFormHeader as an observable, which emits the current form header(updated header)
    *   and can be subscribed by any component.
    */
    formHeaderSource: BehaviorSubject<string>;
    currentFormHeader: import("rxjs").Observable<string>;
    readonly getFormHeader: string;
    setFormHeader: string;
}
